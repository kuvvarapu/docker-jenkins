node {
  stage('Validating developer branch'){
	STAT = developer
	 echo "${STAT}"
     echo 'printenv'
	if ( "${STAT}" == 'developer' ){
		echo 'I only execute on developer branch'
}else {

	echo 'I execute elsewhere'

}
}
}
